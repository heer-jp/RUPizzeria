<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.rupizzeria">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RUPizzeria">
        <activity
            android:name=".StoreOrdersActivity"
            android:label="@string/store_orders"
            android:exported="false"/>
        <activity
            android:name=".CurrentOrderActivity"
            android:label="@string/current_order"
            android:exported="false" />
        <activity
            android:name=".CustomizationActivity"
            android:label="@string/customize_pizza"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>